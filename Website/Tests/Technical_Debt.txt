===========TECHNICAL DEBT===========
1) In every create API, add logic to check if the document already exists in the database or not.
2) Add the DOB parameter to the User schemas and the User documents.
3) Add an appropriate index to each collection of the database.
4) Create a list of factors to be considered / steps to be followed while creating an API.
5) Add XSS sanitization to response handling logic.
6) Find best practices to do fuzz testing on all schemas.
===========TECHNICAL DEBT===========

I have a test in the updateUser.test.js file.  This test is called "Should successfully insert the updated user document into the database via User.findOneAndUpdate()".