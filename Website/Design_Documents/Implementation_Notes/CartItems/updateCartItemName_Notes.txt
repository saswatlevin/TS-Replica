We need the following pieces of data:
* product_id -> Request body | From frontend

updateCartItemName ->
* Create the filter: {"CartItems.product_id": product_id}.
* Create an update object as follows:
 const update_object = {
            $set: {
                "CartItems.$[item].cart_item_name": updated_cart_item_name
            }
        };
* Use updateMany() to update the name of the cart_item across all CartItems of the same product across all users
* Return the result.

NOTE: updateCartItemName() is an express HELPER FUNCTION and not a middleware.

NOTE: updateMany() is used to update the cart_item_name of all CartItems of the same product across all users.

NOTE: No need to check if product exists, since updateProductName already checks that and in the future DB operations will be made atomic.
