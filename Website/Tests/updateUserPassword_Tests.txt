updateUserPassword - Test Plan
DATABASE INSERTION TEST
* Should update the corresponding User document with only the updated password using User.findOneAndUpdate()

DATA TRANSFORMATION TESTS
* Should generate the filter with an object containing user_id.
* Should create the password update object by removing the user_id from the request body.
* Should check if the updated password has been successfully hashed.
* Should check whether the updated password is successfully sent as a response with status code 201.


  